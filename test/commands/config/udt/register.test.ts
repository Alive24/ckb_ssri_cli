import {runCommand} from '@oclif/test'
import {expect} from 'chai'

describe('config:udt:register', () => {
  // TODO: Implement tests
  // it('runs config:udt:register cmd', async () => {
  //   const {stdout} = await runCommand('config:udt:register')
  //   expect(stdout).to.contain('hello world')
  // })

  // it('runs config:udt:register --name oclif', async () => {
  //   const {stdout} = await runCommand('config:udt:register --name oclif')
  //   expect(stdout).to.contain('hello oclif')
  // })
})
